#!/usr/bin/php
<?php

if (file_exists(sprintf('%s/php-cs-fixer', __DIR__))) {
    $binaryDir = '.';
} else {
    $binaryDir = sprintf('%s/../vendor/bin', __DIR__);
}

$binary = realpath(sprintf('%s/php-cs-fixer', $binaryDir));

$configs = [
    '--level' => 'symfony',
    '--fixers' => '-phpdoc_to_comment,-phpdoc_var_without_name,-phpdoc_short_description,-empty_return',
];

foreach ($configs as $name => $value) {
    $_SERVER['argv'][] = sprintf('%s=%s', $name, $value);
}

require_once $binary;